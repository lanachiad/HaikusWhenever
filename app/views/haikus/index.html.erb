<% if @haikus == nil %>
  <h2>No haikus yet. Why don't you create the first one?</h2>
<% else %>
  <% @haikus.each do |haiku| %>
    <p><%= haiku.poem %></p>
    <p><em><%= haiku.user.username %></em></p>
  <% end %>
<% end %>